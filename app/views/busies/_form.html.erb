<%= form_with(model: busy, local: true) do |form| %>
  <% if busy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(busy.errors.count, "error") %> prohibited this busy from being saved:</h2>

      <ul>
      <% busy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reservado %>
    <%= form.datetime_select :reservado %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
